<?php declare(strict_types=1);

namespace App\Application\Write\\{{properCase featureName}}\\{{crudType}}{{properCase featureName}};

class {{crudType}}{{properCase featureName}}Handler
{

    public function __construct(
    ) {
    }

    public function __invoke({{crudType}}{{properCase featureName}} $command)
    {

    }
}
